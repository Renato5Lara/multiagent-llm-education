"""e2e — recorridos extremo a extremo reales del runtime (CLAUDE.md,
actualización 2026-07-12 tercera: "ninguna Plataforma Operativa se
considera cerrada hasta haber sido validada mediante un recorrido E2E
real"). Requiere el servidor real corriendo (`uvicorn app.main:app`) y
PostgreSQL real — no levanta el servidor por sí mismo, ni usa mocks."""
