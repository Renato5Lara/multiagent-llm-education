"""domain.shared — tipos comunes a varias capacidades (BLUEPRINT).

Jamás lógica de coordinación (P3): solo tipos/contratos que una capacidad
importaría de otra si no existiera este paquete. El lint de importación
no restringe `runtime.domain.shared` — es la válvula sancionada.
"""
